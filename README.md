# 🎮 Quiz Battle

Интерактивное веб-приложение для проведения квизов в реальном времени. 
Ведущий показывает экран на телевизоре, игроки подключаются с телефонов и соревнуются, кто первый нажмёт кнопку!

## 📋 Возможности

- ✅ До 20 игроков одновременно
- ✅ Real-time синхронизация через WebSocket
- ✅ Точное определение победителя на сервере (защита от задержки сети)
- ✅ Красивый адаптивный интерфейс для мобильных и TV
- ✅ Эмодзи-аватары для игроков
- ✅ Тактильная обратная связь (вибрация) на мобильных устройствах

## 🛠 Технологии

- **Backend:** Java 17, Spring Boot 3.2, WebSocket (STOMP)
- **Frontend:** React 18, STOMP.js, React Router

## 🚀 Запуск приложения

### Требования
- Java 17+
- Maven 3.6+
- Node.js 18+
- npm 9+

### Backend (Spring Boot)

```bash
cd backend
mvn spring-boot:run
```

Сервер запустится на `http://localhost:8080`

### Frontend (React)

```bash
cd frontend
npm install
npm start
```

Приложение откроется на `http://localhost:3000`

## 📖 Как играть

### Для ведущего:
1. Откройте `http://localhost:3000` на компьютере/телевизоре
2. Нажмите "Создать игру"
3. Добавьте игроков (имя + аватар)
4. Скопируйте ссылки и отправьте игрокам (или покажите на экране QR-код)
5. Когда все подключились - задайте вопрос устно
6. Нажмите "Старт раунда"
7. Первый нажавший отобразится на экране!
8. После ответа нажмите "Новый вопрос" для сброса

### Для игрока:
1. Откройте ссылку, которую дал ведущий
2. Дождитесь начала раунда (кнопка станет зелёной)
3. Нажмите кнопку как можно быстрее!
4. Если успели первым — отвечайте на вопрос!

## 🌐 Сетевая игра

Для игры по локальной сети:

1. Узнайте IP-адрес компьютера с сервером:
   - Windows: `ipconfig`
   - Mac/Linux: `ifconfig` или `ip addr`

2. Измените URL в `frontend/src/services/websocket.js`:
   ```javascript
   const SOCKET_URL = 'http://YOUR_IP:8080/ws';
   ```

3. Или используйте переменную окружения:
   ```bash
   REACT_APP_WS_URL=http://192.168.1.100:8080/ws npm start
   ```

4. Игроки подключаются по адресу: `http://YOUR_IP:3000/play/ROOM_CODE/PLAYER_ID`

## 📁 Структура проекта

```
button_app/
├── backend/                    # Spring Boot приложение
│   ├── src/main/java/com/quizbattle/
│   │   ├── config/            # WebSocket конфигурация
│   │   ├── controller/        # REST и WebSocket контроллеры
│   │   ├── model/             # Модели данных
│   │   └── service/           # Бизнес-логика
│   └── pom.xml
├── frontend/                   # React приложение
│   ├── src/
│   │   ├── pages/             # Страницы (Host, Player, Home)
│   │   ├── services/          # WebSocket сервис
│   │   ├── App.js
│   │   └── App.css
│   └── package.json
└── README.md
```

## 🔧 API

### WebSocket Endpoints

| Endpoint | Описание |
|----------|----------|
| `/app/create-room` | Создать комнату |
| `/app/add-player` | Добавить игрока |
| `/app/join-room` | Подключить игрока |
| `/app/start-round` | Начать раунд |
| `/app/press-button` | Нажать кнопку |
| `/app/reset-round` | Сбросить раунд |

### REST Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/room/{code}` | Проверить комнату |
| GET | `/api/avatars` | Список аватаров |

## 📝 Лицензия

MIT


