# 🎮 Quiz Battle - Интерактивная викторина в реальном времени

Интерактивное веб-приложение для проведения квизов в реальном времени. Ведущий (администратор) показывает экран на телевизоре/компьютере, игроки подключаются с телефонов и соревнуются, кто первый нажмёт кнопку!

## 📋 Возможности

- ✅ До 20 игроков одновременно
- ✅ Real-time синхронизация через WebSocket (STOMP)
- ✅ Точное определение победителя на сервере (защита от задержки сети)
- ✅ Красивый адаптивный интерфейс для мобильных и TV
- ✅ Система ролей: Администратор (создание комнат) и Игроки
- ✅ Автоматическое заполнение пользователей из аватаров
- ✅ Отображение аватаров пользователей
- ✅ Работа по локальной сети (доступ с телефонов)

## 🛠 Технологии

- **Backend:** Java 17, Spring Boot 3.2, WebSocket (STOMP), Spring Data JPA, PostgreSQL
- **Frontend:** React 19, STOMP.js, React Router DOM v7
- **Database:** PostgreSQL 15
- **Containerization:** Docker, Docker Compose

## 📦 Требования

### Для запуска через Docker Compose (рекомендуется):
- Docker Desktop (Windows/Mac) или Docker Engine (Linux)
- Docker Compose v2.0+

### Для локального запуска:
- Java 17+
- Maven 3.6+
- Node.js 18+
- npm 9+
- PostgreSQL 15+ (или Docker для запуска только БД)

## 🚀 Быстрый старт (Docker Compose)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd land35
```

### 2. Запуск всех сервисов

```bash
docker-compose up --build -d
```

Эта команда:
- Соберёт образы backend и frontend
- Запустит PostgreSQL, Backend и Frontend
- Настроит сеть между контейнерами
- Автоматически заполнит базу данных пользователями из папки `backend/src/main/resources/avatars/`

### 3. Проверка статуса

```bash
docker-compose ps
```

Должны быть запущены 3 контейнера:
- `quiz-battle-db` (PostgreSQL) - порт 5432
- `quiz-battle-backend` (Spring Boot) - порт 8080
- `quiz-battle-frontend` (Nginx) - порт 3000

### 4. Доступ к приложению

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8080
- **WebSocket:** ws://localhost:8080/ws

### 5. Остановка сервисов

```bash
docker-compose down
```

Для удаления всех данных (включая БД):
```bash
docker-compose down -v
```

## 🔧 Локальный запуск (для разработки)

### Backend

1. **Запуск PostgreSQL через Docker:**
```bash
docker-compose up -d postgres
```

2. **Настройка базы данных:**
   - База данных создаётся автоматически при первом запуске
   - Настройки в `backend/src/main/resources/application.properties`:
     ```
     spring.datasource.url=jdbc:postgresql://localhost:5432/quizbattle
     spring.datasource.username=quizbattle
     spring.datasource.password=quizbattle123
     ```

3. **Сборка и запуск:**
```bash
cd backend
mvn clean package -DskipTests
mvn spring-boot:run
```

Backend запустится на `http://localhost:8080`

### Frontend

1. **Установка зависимостей:**
```bash
cd frontend
npm install
```

2. **Запуск в режиме разработки:**
```bash
npm start
```

Frontend откроется на `http://localhost:3000`

## 👥 Система пользователей

### Роли

- **ADMIN** - может создавать комнаты (по умолчанию: пользователь `master35`)
- **PLAYER** - может присоединяться к комнатам

### Автоматическое заполнение

При первом запуске приложения автоматически создаются пользователи:
- Из файлов в `backend/src/main/resources/avatars/` (например: `alex.png`, `anna.png`)
- Каждому файлу соответствует пользователь с полным именем
- Специальный пользователь `master35` создаётся с ролью ADMIN

### Вход в систему

1. Откройте http://localhost:3000
2. Вы будете перенаправлены на страницу логина
3. Выберите пользователя из списка
4. Введите пароль: `password123` (для всех пользователей)
5. После входа:
   - **ADMIN** → перенаправление на `/host` (создание комнаты)
   - **PLAYER** → перенаправление на `/join` (присоединение к комнате)

## 🎮 Как играть

### Для ведущего (ADMIN):

1. Войдите как администратор (пользователь `master35` или другой с ролью ADMIN)
2. Нажмите "Create Game" на главной странице
3. Комната будет создана автоматически, все игроки (не админы) будут добавлены
4. Откройте страницу на телевизоре/большом экране
5. Когда все игроки подключились:
   - Задайте вопрос устно
   - Нажмите "Старт раунда"
   - Первый нажавший отобразится на экране!
6. После ответа нажмите "Новый вопрос" для сброса

### Для игрока (PLAYER):

1. Войдите как игрок (любой пользователь с ролью PLAYER)
2. Нажмите "Join Game" на главной странице
3. Введите код комнаты (отображается на экране ведущего)
4. Подождите начала раунда (кнопка станет зелёной)
5. Нажмите кнопку как можно быстрее!
6. Если успели первым — отвечайте на вопрос!

## 📱 Доступ с мобильных устройств

### Настройка для локальной сети

1. **Узнайте IP адрес компьютера:**
   - Windows: `ipconfig | findstr IPv4`
   - Mac/Linux: `ip addr` или `ifconfig`
   - Пример: `192.168.50.31`

2. **Откройте на телефоне:**
   ```
   http://ВАШ_IP:3000
   ```
   Например: `http://192.168.50.31:3000`

3. **Убедитесь, что:**
   - Компьютер и телефон в одной Wi-Fi сети
   - Брандмауэр Windows разрешает подключения на портах 3000 и 8080

### Настройка брандмауэра Windows

```powershell
# Разрешить входящие подключения на портах
New-NetFirewallRule -DisplayName "Quiz Battle Frontend" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "Quiz Battle Backend" -Direction Inbound -LocalPort 8080 -Protocol TCP -Action Allow
```

### Автоматическое определение URL

Приложение автоматически определяет URL API и WebSocket на основе хоста:
- При открытии с `localhost` → используется `localhost:8080`
- При открытии с IP адреса → используется `IP:8080`

Это работает благодаря утилите `frontend/src/utils/api.js`.

## 📁 Структура проекта

```
land35/
├── backend/                          # Spring Boot приложение
│   ├── src/main/java/com/quizbattle/
│   │   ├── config/                   # Конфигурация (CORS, WebSocket, DataLoader)
│   │   ├── controller/               # REST и WebSocket контроллеры
│   │   │   ├── GameController.java  # WebSocket обработка игры
│   │   │   ├── UserController.java  # REST API для пользователей
│   │   │   ├── RestController.java  # Общие REST endpoints
│   │   │   └── AvatarController.java # Отдача аватаров
│   │   ├── dto/                     # Data Transfer Objects
│   │   ├── model/                   # Модели данных
│   │   │   ├── entity/              # JPA Entity (User, RoomEntity, RoomInvitation)
│   │   │   ├── GameState.java       # Состояние игры
│   │   │   ├── Player.java          # Модель игрока
│   │   │   └── Room.java            # Модель комнаты
│   │   ├── repository/              # Spring Data JPA репозитории
│   │   ├── service/                 # Бизнес-логика
│   │   │   ├── GameService.java     # Логика игры
│   │   │   └── UserService.java     # Логика пользователей
│   │   └── QuizBattleApplication.java
│   ├── src/main/resources/
│   │   ├── avatars/                  # PNG файлы аватаров (автоматически загружаются)
│   │   └── application.properties    # Настройки приложения
│   ├── Dockerfile
│   └── pom.xml
├── frontend/                         # React приложение
│   ├── src/
│   │   ├── components/              # React компоненты
│   │   │   └── AvatarDisplay.js     # Компонент отображения аватара
│   │   ├── pages/                   # Страницы приложения
│   │   │   ├── HomePage.js          # Главная страница
│   │   │   ├── LoginPage.js         # Страница входа
│   │   │   ├── HostPage.js          # Страница ведущего
│   │   │   ├── PlayerPage.js         # Страница игрока
│   │   │   └── JoinRoomPage.js      # Страница присоединения
│   │   ├── services/                # Сервисы
│   │   │   └── websocket.js         # WebSocket клиент
│   │   ├── utils/                   # Утилиты
│   │   │   └── api.js               # Определение URL API
│   │   ├── App.js                   # Главный компонент
│   │   └── App.css                  # Стили
│   ├── Dockerfile
│   ├── nginx.conf                   # Конфигурация Nginx
│   └── package.json
├── docker-compose.yml               # Конфигурация Docker Compose
└── README.md                        # Этот файл
```

## 🔌 API Endpoints

### REST API

#### Пользователи

| Метод | Endpoint | Описание | Требования |
|-------|----------|----------|------------|
| GET | `/api/users` | Получить список всех пользователей | - |
| POST | `/api/users/register` | Регистрация нового пользователя | Body: `RegisterRequest` |
| POST | `/api/users/login` | Авторизация пользователя | Body: `LoginRequest` |
| GET | `/api/users/{id}` | Получить информацию о пользователе | - |

#### Приглашения

| Метод | Endpoint | Описание | Требования |
|-------|----------|----------|------------|
| POST | `/api/users/invitations` | Создать приглашение | Query: `hostUserId`, Body: `InvitationRequest` |
| GET | `/api/users/{userId}/invitations` | Получить список приглашений пользователя | - |
| POST | `/api/users/invitations/{invitationId}/accept` | Принять приглашение | Query: `userId` |
| POST | `/api/users/invitations/{invitationId}/reject` | Отклонить приглашение | Query: `userId` |
| GET | `/api/users/rooms/{roomCode}/invitations` | Получить приглашения для комнаты | - |

#### Комнаты

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/room/{code}` | Проверить существование комнаты |

#### Аватары

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/avatars/{filename}` | Получить изображение аватара |

### WebSocket Endpoints (STOMP)

Все WebSocket сообщения отправляются на `/app/*` и подписываются на `/topic/*`

| Endpoint | Направление | Описание | Payload |
|----------|-------------|----------|---------|
| `/app/create-room` | → | Создать комнату | `{ userId: number }` |
| `/app/join-room` | → | Подключить игрока к комнате | `{ roomCode: string, playerId: string }` |
| `/app/start-round` | → | Начать раунд | `{ roomCode: string }` |
| `/app/press-button` | → | Нажать кнопку | `{ roomCode: string, playerId: string, clientTimestamp: number }` |
| `/app/reset-round` | → | Сбросить раунд | `{ roomCode: string }` |
| `/app/get-room-state` | → | Получить состояние комнаты | `{ roomCode: string }` |
| `/topic/room/{roomCode}` | ← | Обновления состояния комнаты | `GameState` |
| `/topic/room/{roomCode}/players` | ← | Обновления списка игроков | `Player[]` |

## 🔐 Переменные окружения

### Backend

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `SPRING_PROFILES_ACTIVE` | Профиль Spring | `docker` (в Docker) |
| `SPRING_DATASOURCE_URL` | URL базы данных | `jdbc:postgresql://postgres:5432/quizbattle` |
| `SPRING_DATASOURCE_USERNAME` | Имя пользователя БД | `quizbattle` |
| `SPRING_DATASOURCE_PASSWORD` | Пароль БД | `quizbattle123` |

### Frontend

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `REACT_APP_API_URL` | URL API бэкенда | Автоматически определяется |
| `REACT_APP_WS_URL` | URL WebSocket | Автоматически определяется |

## 🐛 Решение проблем

### Проблема: "Не удалось загрузить список пользователей"

**Решение:**
1. Проверьте, что backend запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs backend`
3. Убедитесь, что порт 8080 не занят другим приложением
4. Проверьте брандмауэр Windows

### Проблема: WebSocket не подключается

**Решение:**
1. Проверьте, что используется правильный URL (не localhost при доступе с телефона)
2. Проверьте логи backend: `docker-compose logs backend`
3. Убедитесь, что порт 8080 доступен

### Проблема: База данных не подключается

**Решение:**
1. Проверьте статус PostgreSQL: `docker-compose ps postgres`
2. Проверьте логи: `docker-compose logs postgres`
3. Убедитесь, что порт 5432 не занят
4. Пересоздайте контейнер: `docker-compose up -d --force-recreate postgres`

### Проблема: Аватары не отображаются

**Решение:**
1. Проверьте, что файлы PNG находятся в `backend/src/main/resources/avatars/`
2. Пересоберите backend: `docker-compose build backend`
3. Проверьте доступность: `http://localhost:8080/avatars/alex.png`

### Проблема: Не могу создать комнату

**Решение:**
1. Убедитесь, что вы вошли как пользователь с ролью ADMIN
2. Проверьте логи backend для ошибок
3. Убедитесь, что `userId` передаётся в запросе создания комнаты

## 📝 Логи и отладка

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend

# Только база данных
docker-compose logs -f postgres
```

### Пересборка образов

```bash
# Пересобрать все сервисы
docker-compose build --no-cache

# Пересобрать только backend
docker-compose build --no-cache backend

# Пересобрать только frontend
docker-compose build --no-cache frontend
```

## 🚀 Развёртывание в продакшн

### Рекомендации:

1. **Измените пароли БД** в `docker-compose.yml`
2. **Настройте HTTPS** для frontend (nginx)
3. **Используйте переменные окружения** для секретов
4. **Настройте резервное копирование** базы данных
5. **Мониторинг** и логирование

### Пример .env файла:

```env
POSTGRES_PASSWORD=your_secure_password
SPRING_DATASOURCE_PASSWORD=your_secure_password
REACT_APP_API_URL=https://api.yourdomain.com
```

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

### Добавление нового пользователя

1. Добавьте PNG файл в `backend/src/main/resources/avatars/`
2. Имя файла будет использовано как username
3. Полное имя генерируется автоматически
4. При следующем запуске пользователь будет создан автоматически

### Изменение ролей

Роли определяются в `backend/src/main/java/com/quizbattle/model/entity/UserRole.java`:
- `ADMIN` - может создавать комнаты
- `PLAYER` - может присоединяться к комнатам

По умолчанию пользователь `master35` имеет роль ADMIN.

---

**Приятной игры! 🎉**
